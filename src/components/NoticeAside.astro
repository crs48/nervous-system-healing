---
/** An invitational noticing prompt (0004): causes the noticing by being read;
    always carries an exit. */
---

<aside
  class="rounded-card bg-[var(--surface-panel)]/50 p-5 my-8 max-w-[65ch]"
>
  <p class="text-sm font-bold m-0 mb-1 text-[var(--heading)]">
    A small experiment — entirely optional
  </p>
  <div><slot /></div>
</aside>
